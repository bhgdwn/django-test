apiVersion: v1
kind: PersistentVolume
metadata:
  name: mysql-pv-nfs
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteMany   # NFS supports multiple nodes
  storageClassName: nfs-client
  persistentVolumeReclaimPolicy: Retain
  nfs:
    path: /srv/nfs/mysql    # Path exported on your NFS server
    server: 192.168.56.11 

